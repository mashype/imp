%h1 All Loaded Providers

%table.table.table-striped
  %thead
    %tr
      %th Org name
      %th{:colspan => "3"}
  %tbody
    - @providers.each do |provider|
      %tr
        %td= link_to provider.org_name, provider
%br/
= link_to 'Add New Provider', new_provider_path, class: "btn btn-default" 

